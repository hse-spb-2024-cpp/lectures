#include <iostream>
#include <iostream>  // NOLINT

int main() {
    bool foo = true;
    int x = 10;
    if (2 * 2 == 4 && x == x && static_cast<long long>(x * x) == 100) {
        std::cout << "Hello\n";
    } else {
        std::cout << "Hello\n";
    }
}
